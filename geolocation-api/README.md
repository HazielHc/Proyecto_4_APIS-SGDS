# Geolocation API

This mini project runs a small local web app that uses Open-Meteo geocoding and forecast endpoints to display live weather for a city.

## Run

```bash
npm start
```

Then open:

```bash
http://localhost:3003
```
