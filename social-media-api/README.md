# Social Media API

This mini project runs a small local web app that consumes the DEV.to API and displays recent articles for a given tag.

## Run

```bash
npm start
```

Then open:

```bash
http://localhost:3001
```
